# Synthesis (Yosys)

This folder contains a simple Yosys synthesis script for `uart_full`.

## Running

From the repo root:

- `yosys -s examples/uart/uart_full/synthesis/synth.ys`

Outputs:

- `uart_full_synth.v`
- `synth.rpt`
