# Synthesis (Yosys)

This folder contains a simple Yosys synthesis script for `uart_tx`.

## Running

From the repo root:

- `yosys -s examples/uart_tx/synthesis/synth.ys`

Outputs:

- `uart_tx_synth.v`
- `synth.rpt`
